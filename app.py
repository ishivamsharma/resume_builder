from resume import app

if __name__ ==  '__main__':
    app.run(host="0.0.0.0",debug=True)
    """
     host="0.0.0.0" will make the page accessableby going to http://[ip]:5000/ on 
     any computer in the network.
    """
